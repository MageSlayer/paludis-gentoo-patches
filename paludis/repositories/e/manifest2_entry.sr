#!/usr/bin/env bash
# vim: set sw=4 sts=4 et :

make_class_Manifest2Entry()
{
    visible

    key type "std::string"
    key name "std::string"
    key size "off_t"

    key sha1 "std::string"
    key sha256 "std::string"
    key rmd160 "std::string"
    key md5 "std::string"

    allow_named_args
}
